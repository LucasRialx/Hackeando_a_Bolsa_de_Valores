[build]
  publish = "public"  # Certifique-se de que este é o diretório correto para o conteúdo estático
  command = "echo 'No build required'"

[[redirects]]
  from = "/api/*"
  to = "/index.html"
  status = 200
  force = true
